# SELA Cabinets environment variables
# Copy this file to the appropriate local/deployment environment file.
# Keep every value blank in source control. Store real values only in approved secret storage.

# Runtime mode: development, test, or production.
NODE_ENV=

# Canonical public HTTPS origin. Required in production; do not include credentials, query, or fragment.
NEXT_PUBLIC_APP_URL=

# Primary PostgreSQL connection URL. Required in production with sslmode=verify-full.
DATABASE_URL=

# Optional PEM-encoded private CA for a database that still uses verify-full.
DATABASE_CA_CERT=

# Independent signing secrets. Both must be at least 32 characters and must not match.
ADMIN_SECRET=
USER_AUTH_SECRET=

# Development-only authorization contract fixture. Never configure in test or production.
SELA_AUTH_CONTRACT_USER_ID=

# Reserved launch gate. Only false is accepted until the customer portal security work is complete.
ENABLE_CUSTOMER_PORTAL=

# Google OAuth. Client ID and secret must be configured together; admin OAuth also requires the allowlist.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
ADMIN_GOOGLE_EMAILS=

# OpenAI server API credential for chat.
OPENAI_API_KEY=

# Resend server API credential and lead-notification recipient.
RESEND_API_KEY=
OWNER_EMAIL=

# Optional Notion CRM integration; configure both values together.
NOTION_API_KEY=
NOTION_DATABASE_ID=

# Supabase public project settings and server-only service credential.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google Analytics 4 public measurement identifier.
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Public Calendly event URLs. Leave blank until verified booking links are approved.
NEXT_PUBLIC_CALENDLY_PHONE_CONSULTATION=
NEXT_PUBLIC_CALENDLY_INHOME_MEASUREMENT=
NEXT_PUBLIC_CALENDLY_VIRTUAL_DESIGN=

# Optional public Google Maps browser key. Restrict it by domain and API in Google Cloud.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
