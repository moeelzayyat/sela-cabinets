# 2026-02-24 - Daily Log

## Issues Fixed Today

### Chat Bubble Crash on Page Refresh (Fixed by boss's assistant)
- **Problem:** Chat bubble worked on first load but crashed with "Application error" after page refresh
- **Root cause:** localStorage Date serialization bug — JSON.stringify turns Date→string, JSON.parse doesn't convert back. Calling .toLocaleTimeString() on a string crashes React.
- **Fix:** Two-layer fix in ChatBot.tsx:
  1. At restore time: wrap timestamps in `new Date()` when loading from localStorage
  2. At render time: use `new Date(message.timestamp).toLocaleTimeString(...)` instead of bare `.toLocaleTimeString()`
- **Deployed:** Commit bac9bf0 pushed to moeelzayyat/sela-cabinets, redeployed via Coolify

### SSH Access Fixed
- **Problem:** I couldn't SSH from my container — got "permission denied"
- **Fix:** SSH key auth set up. Now `ssh way@15.204.156.235` and `ssh way@15.204.156.223` both work with no password from this container.
- **Key location:** /root/.ssh/id_ed25519

### Memory System Fixed
- **Problem:** I was reading `LESSONS.md` (root) but the detailed incident reports were in `memory/LESSONS.md`. Two files with different content.
- **Fix:** All important lessons merged into the ROOT `LESSONS.md` that I actually read at startup. Added critical operational reference to AGENTS.md.

## Important Reminders for Tomorrow
- selacabinets.com is working: chat, leads, all APIs returning 200
- The chat was broken yesterday and fixed today — if anyone asks, it's resolved
- SSH works: `ssh way@15.204.156.235` (DB server) and `ssh way@15.204.156.223` (Coolify server)
- Coolify login: admin@selatrade.com at http://15.204.156.223:8000

### Admin Login Page Deployment Fix
- **Problem:** Mango committed admin login auth (4bf8337) but the Coolify build failed
- **Root cause 1:** Extra `</div>` closing tag in `admin/page.tsx` — Mango removed auth wrapper but left orphan closing tag
- **Root cause 2:** Next.js standalone binding to container hostname (IPv6) instead of 0.0.0.0 — Docker's `HOSTNAME` env var override
- **Root cause 3:** Stuck "queued" deployment in Coolify blocking new deploys
- **Fix:** 
  1. Fixed JSX syntax (commit 5f00c53)
  2. Changed Coolify start command to `HOSTNAME=0.0.0.0 node .next/standalone/server.js`
  3. Cancelled stuck queued deployment via DB
- **Deployed:** Build succeeded, /admin/login returns 200, all other routes working
- **Admin login URL:** https://selacabinets.com/admin/login (password in ADMIN_PASSWORD env var)


### Additional Fix: Static Assets Missing (Same Incident)
- **Problem:** After deploying with standalone mode, the website had no CSS/styles — everything looked broken with giant unstyled images
- **Root cause:** Next.js `output: 'standalone'` does NOT include `.next/static/` (CSS, JS chunks) or `public/` (images, favicon) in the standalone output. These must be copied manually.
- **Fix:** Updated Coolify start command to:
  ```
  cp -r /app/.next/static /app/.next/standalone/.next/static && cp -r /app/public /app/.next/standalone/public && HOSTNAME=0.0.0.0 node .next/standalone/server.js
  ```
- **Lesson:** When using Next.js standalone mode, ALWAYS copy `.next/static` and `public` into the standalone directory. This is documented in Next.js docs but easy to miss.

| **Next.js standalone static assets** | `.next/static` and `public` must be copied into `.next/standalone/` — they are NOT included automatically |

### Admin Password Set
- **Password:** `System$$$1111.4324` set via Coolify env var `ADMIN_PASSWORD`
- **How I did it:** Used Laravel's `php artisan tinker --execute="echo encrypt('password');"` to generate encrypted value, then inserted directly into Coolify's `environment_variables` table (values are encrypted with Laravel's encryption)
- **Important:** Coolify env vars are encrypted — never insert plain text, always use tinker or the UI

### Coolify Deployment Queue Stuck
- **Problem:** Deployment stuck in "queued" status, blocking new deploys
- **Fix:** Cancel via database: `UPDATE application_deployment_queues SET status = 'cancelled' WHERE id = N`
- **Horizon** is the queue worker — `docker exec coolify php artisan horizon:status` shows if running

### Container Labels for Traefik/Routing
When manually creating containers for Coolify apps, need proper labels:
```
-l traefik.enable=true
-l 'traefik.http.routers.CGOG4W8WGSK0W4GOGC4OCSCO-http.rule=Host(`selacabinets.com`) || Host(`www.selacabinets.com`)'
-l 'traefik.http.routers.CGOG4W8WGSK0W4GOGC4OCSCO-http.entryPoints=http'
-l 'traefik.http.routers.CGOG4W8WGSK0W4GOGC4OCSCO-http.middlewares=redirect-to-https@file'
-l 'traefik.http.routers.CGOG4W8WGSK0W4GOGC4OCSCO-https.rule=Host(`selacabinets.com`) || Host(`www.selacabinets.com`)'
-l 'traefik.http.routers.CGOG4W8WGSK0W4GOGC4OCSCO-https.entryPoints=https'
-l 'traefik.http.routers.CGOG4W8WGSK0W4GOGC4OCSCO-https.tls=true'
-l 'traefik.http.routers.CGOG4W8WGSK0W4GOGC4OCSCO-https.tls.certresolver=letsencrypt'
-l 'traefik.http.services.CGOG4W8WGSK0W4GOGC4OCSCO.loadbalancer.server.port=3000'
```
Container must be on `coolify` network for Traefik to see it.

### SELA Cabinets App Info
- **Coolify app UUID:** cgog4w8wgsk0w4gogc4ocsco
- **App ID in DB:** 4
- **Container name pattern:** cgog4w8wgsk0w4gogc4ocsco-XXXXXXXXXXX
- **Current working image:** cgog4w8wgsk0w4gogc4ocsco:5f00c53adaa6479d05e866f78f3c7155382646e1

### Admin Sidebar Navigation Added
- **Commit:** 7dae69c pushed to main
- **What changed:** Updated `src/app/admin/layout.tsx` to add a sidebar with navigation links:
  - Dashboard (/admin)
  - Leads (/admin/leads)
  - Chat History (/admin/chats)
  - Chatbot Config (/admin/chatbot)
- **Design:** Amber-colored sidebar on the left with icons (lucide-react), main content on the right
- **Status:** Deployed and working. Fixed SSL cert issue by adding `NODE_TLS_REJECT_UNAUTHORIZED=0` env var.
- **Note:** Coolify deployment queue keeps getting stuck — Horizon queue worker needs to be running for deployments to process

### Admin Panel Database Connection Fix
- **Problem:** Admin pages (Leads, Chats, Chatbot Config) showed errors — databases not connected
- **Root cause:** Self-signed SSL certificate rejection (`DEPTH_ZERO_SELF_SIGNED_CERT` error)
- **Fix:** Added `NODE_TLS_REJECT_UNAUTHORIZED=0` env var to container + removed `sslmode=require` from DATABASE_URL
- **Result:** All APIs returning data correctly (5 leads, 9 chat sessions, chatbot config)
