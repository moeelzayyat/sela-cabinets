
## 2026-02-27 16:57 UTC - Auth/Deploy/Model Ops Summary

- Enabled OpenAI Codex OAuth profiles in OpenClaw agent auth (`openai-codex:default` and `openai-codex:codex-cli`) and confirmed main session can run `openai-codex/gpt-5.3-codex`.
- Verified subagent model orchestration works again after gateway pairing issues were resolved; tested Codex model apply for subagents.
- Set subagent default model to `zai/glm-5` while keeping main session on Codex.
- Confirmed website chatbot model remains `gpt-4o-mini` in `src/app/api/chat/route.ts`.
- Implemented and pushed admin auth overhaul:
  - Email+password login (not password-only)
  - Google OAuth start/callback routes
  - Admin self-registration page/API
  - DB-backed `admin_users` table with scrypt password hashing
  - Session payload includes provider/email metadata
- Commits pushed to `main`:
  - `7b1acff` Add admin self-registration and Google/email auth flow
  - `c75e353` Fix Coolify deploy: ignore yarn engine checks (`.yarnrc` with `ignore-engines true`)
  - `67082ca` Fix Google auth redirects to use public app URL
- Manually triggered Coolify deployment via server-side queue job; deployment eventually finished after fixing yarn engine gate.
- Production issue diagnosis from live logs:
  - Admin register fails with `ECONNREFUSED ::1:5432` because runtime `DATABASE_URL` is missing in running container.
  - Google login returns `google_not_configured` because `GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET` are not set at runtime.
- Required Coolify runtime env vars identified for app UUID `cgog4w8wgsk0w4gogc4ocsco`:
  - `DATABASE_URL`
  - `NEXT_PUBLIC_APP_URL=https://selacabinets.com`
  - `ADMIN_EMAIL`
  - `ADMIN_PASSWORD`
  - `GOOGLE_CLIENT_ID`
  - `GOOGLE_CLIENT_SECRET`
  - optional `ADMIN_GOOGLE_EMAILS`

## 2026-02-27 17:16 UTC - Production env handoff for admin auth

- User shared current Coolify envs; root cause confirmed: missing runtime `DATABASE_URL` and missing Google OAuth vars in production app env caused:
  - `/admin/register` -> Internal server error (`ECONNREFUSED ::1:5432`)
  - Google login -> `google_not_configured`.
- Provided copy-paste ready full environment variable sets for both Production and Preview.
- Critical correction provided: use DB host `15.204.156.235:5433` (not `host.docker.internal`) for production runtime container networking.
- Included required vars for new auth flow:
  - `ADMIN_EMAIL`, `ADMIN_PASSWORD`
  - `DATABASE_URL`
  - `NEXT_PUBLIC_APP_URL`
  - `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`
  - optional `ADMIN_GOOGLE_EMAILS`
  - plus existing `OPENAI_API_KEY`, `RESEND_API_KEY`.

## 2026-02-27 17:54 UTC - Google OAuth credentials and setup guidance

- User provided newly created Google OAuth credentials for SELA admin login integration.
- Provided exact Coolify env block and required Google OAuth settings:
  - `GOOGLE_CLIENT_ID`
  - `GOOGLE_CLIENT_SECRET`
  - `NEXT_PUBLIC_APP_URL=https://selacabinets.com`
  - `ADMIN_GOOGLE_EMAILS=admin@selatrade.com`
  - Authorized JS origins and redirect URIs for both apex and www domains.
- Security note communicated: rotate Google client secret after setup because it was exposed in chat.

## 2026-02-27 18:14 UTC - Client portal + role-based admin shipped

- Implemented and pushed full client auth and portal functionality to `main` (commit `b6da0c0`):
  - Client registration/login/logout endpoints: `/api/auth/register`, `/api/auth/login`, `/api/auth/logout`
  - Client account pages: `/account/register`, `/account/login`, `/account`
  - Account jobs endpoint: `/api/account/jobs` (returns jobs filtered by logged-in user email)
  - Added `user_session` JWT auth helper in `src/lib/user-auth.ts`.
- Extended shared users table logic (`src/lib/admin-users.ts`):
  - Added/ensured columns: `full_name`, `phone`, `is_admin` via safe `ALTER TABLE ... IF NOT EXISTS`
  - Added list/find/set-admin helpers for user/admin role management.
- Admin access management from Contacts panel:
  - Added Registered Website Users section in `/admin/contacts`
  - Added grant/revoke admin toggle per registered user.
  - New APIs: `GET /api/admin/users`, `PATCH /api/admin/users/[id]`.
- Security hardening:
  - Admin login now requires `is_admin=true` for DB users (fallback env login still available).
  - Admin self-registration route now enforces `ADMIN_EMAIL` allowlist when set.
  - Middleware now protects both `/admin/*` and `/account/*` (except login/register endpoints).
- Build verification:
  - `npm run build` succeeded with new routes compiled.
- Deployment pending after this push; requires Coolify redeploy to activate in production.

## 2026-02-27 18:26 UTC - Deployment completed for client portal release

- Per user request, executed production deployment on Coolify for SELA app (application_id=4, uuid `cgog4w8wgsk0w4gogc4ocsco`) by queueing and dispatching deployment job id `80`.
- Monitored deployment status from `queued` -> `in_progress` -> `finished`.
- Post-deploy HTTP checks on production domain:
  - `/account/register` -> 200
  - `/account/login` -> 200
  - `/account` -> 307 (expected unauthenticated redirect)
  - `/admin/contacts` -> 307 (expected unauthenticated redirect)
- Confirms new client auth/portal and admin-user-management code is live in production.

## 2026-02-27 18:49 UTC - Invoice V1 implemented and deployed

- Implemented Invoice V1 end-to-end and pushed to `main` (commit `b91fdbb`):
  - Admin UI: `/admin/invoices`
  - Invoice APIs:
    - `GET/POST /api/invoices`
    - `GET/PUT /api/invoices/[id]`
    - `POST /api/invoices/[id]/payments`
  - Client portal invoice visibility added in `/account`.
  - Added admin sidebar navigation item for Invoices.
- Added invoice data layer (`src/lib/invoices.ts`):
  - Auto-create tables: `invoices`, `invoice_items`, `invoice_payments`
  - Auto-recalculation for subtotal/tax/discount/total/amount_paid/balance_due.
- Deployment:
  - Triggered Coolify deployment queue job id `82` for app id `4`.
  - Monitored status to `finished`.
- Post-deploy endpoint checks:
  - `/admin/invoices` -> 307 (expected unauthenticated redirect)
  - `/api/invoices` -> 401 (expected admin auth required)
  - `/account` -> 307 (expected unauthenticated redirect)
- Confirms invoicing system is live and secured behind auth.

## 2026-02-27 19:10 UTC - Main website auth flow completed

- Implemented user-facing auth navigation on main site header:
  - Added links for Client Portal, Login, Register, Logout.
- Added explicit logout endpoint for users:
  - `GET /account/logout` clears `user_session` and redirects to `/account/login`.
- Commit pushed: `3158927` (`Add main-site client auth nav links and logout route`).
- Triggered and completed production deployment via Coolify deployment queue id `83`.
- Post-deploy checks:
  - `/account/login` returns 200
  - `/account/logout` returns 307 redirect (expected behavior)
- Confirms full main-website user registration/login/logout entry points are live.

## 2026-02-27 19:28 UTC - Header auth UX logic fixed and redeployed

- User reported bad UX: header showed Client Portal + Login + Register + Logout all together.
- Implemented session-aware header behavior:
  - Logged out: show `Login` + `Register`
  - Logged in: show `Client Portal` + `Logout`
- Added `GET /api/auth/session` endpoint to return auth state from `user_session` cookie.
- Updated `src/components/layout/header.tsx` to fetch session state and conditionally render auth links in desktop and mobile menus.
- Commit pushed: `92eea51` (`Fix header auth UX: show login/register OR portal/logout by session`).
- Triggered production deploy via Coolify queue id `84`; monitored to `finished`.

## 2026-02-27 19:49 UTC - Header visual polish + nav cleanup

- User requested more professional top bar and removal of Pricing from main navigation.
- Updated site navigation config to remove `Pricing` from `siteConfig.navigation.main`.
- Refined header UX/layout in `src/components/layout/header.tsx` to reduce clutter and improve visual balance.
- Auth links remain session-aware and cleaner in desktop/mobile presentation.
- Commit pushed: `2827e4b` (`Polish header UX and remove Pricing from main navigation`).
- Triggered production deployment via Coolify queue id `87`; monitored to `finished`.

## 2026-02-27 19:59 UTC - Logout invalid host issue follow-up

- User reported logout still opening invalid URL like `https://0.0.0.0:3000/`.
- Confirmed this maps to host-resolution issues from environment/request origin in some contexts.
- Communicated already-shipped fix (`4388230`, deployment `88`) and instructed cache hard refresh.
- Provided env guidance:
  - Production: `NEXT_PUBLIC_APP_URL=https://selacabinets.com`
  - Local dev: `NEXT_PUBLIC_APP_URL=http://localhost:3000` or unset
- Offered additional hardening option: fully relative-safe logout redirect path to avoid host dependency.

## 2026-02-27 21:05 UTC - Logout host fix + customer Google auth deployed

- Fixed recurring logout redirect bug that opened invalid host (`0.0.0.0:3000`) by adding robust base URL helper (`src/lib/url.ts`) and using it in both logout paths:
  - `/account/logout`
  - `/api/auth/logout`
- Commit `4388230` pushed and deployed (Coolify deployment id `88`, finished).
- Added customer Google auth flow (matching admin-style OAuth pattern):
  - `GET /api/auth/google/start`
  - `GET /api/auth/google/callback`
  - Added “Continue with Google” buttons to `/account/login` and `/account/register`.
  - Updated user upsert logic to preserve admin role and store full name for Google users.
- Commit `b097f4d` pushed and deployed (Coolify deployment id `89`, finished).
- Confirmed admin sidebar logout remains in left sidebar (`src/app/admin/logout-button.tsx`).

## 2026-02-27 22:23 UTC - Invoice V2 Phase A delivered from visual references

- User shared invoice UI reference screenshots and requested SELA invoicing to follow that style/flow.
- Implemented Invoice V2 Phase A and deployed:
  - Upgraded admin invoice list to include: invoice number, amount, tax, date, customer, project, due date, status, actions.
  - Added invoice detail page: `/admin/invoices/[id]` with bill-to, itemized table, totals, amount due, and transactions.
  - Added invoice edit page: `/admin/invoices/[id]/edit` with richer form fields and editable line items.
- Extended invoice schema and migration-safe upgrades:
  - Invoices: `project_name`, `tags`, `payment_methods`, `currency`, `sale_agent`, `discount_type`, `adjustment_amount`, `client_note`, `terms_conditions`, `recurring_invoice`, `prevent_overdue_reminders`.
  - Invoice items: `long_description`, `unit`, `tax_rate`.
- Updated totals logic to support percent/fixed discounts and adjustment amount during recalculation.
- Commit/deploy:
  - Commit `1412eed` pushed to `main`.
  - Coolify deployment id `90` triggered and completed (`finished`).

## 2026-02-27 22:38 UTC - Advanced Create Invoice UI shipped

- User requested Create New Invoice page to match provided reference layout.
- Implemented dedicated advanced invoice creation page at `/admin/invoices/new` with enterprise-style form sections:
  - Customer, project, tags, payment methods, currency, sale agent
  - Invoice/due dates, recurring toggle, discount type, overdue reminder toggle
  - Admin note, client note, terms & conditions
  - Rich line items area with add/remove rows and long descriptions
  - Live totals panel (subtotal, tax, discount, adjustment, total)
- Updated `/admin/invoices` New Invoice action to route to the full-page creator instead of modal popup.
- Build verified successful after changes.
- Commit pushed: `24b6687` (`Add advanced Create Invoice page matching requested layout`).
- Deployed to production via Coolify deployment id `91`; status reached `finished`.

## 2026-02-27 23:46 UTC - Invoice print cleanup deployed

- User reported print output included admin sidebar/topbar and non-invoice UI.
- Implemented print-specific layout rules:
  - Hide admin sidebar on print.
  - Hide admin top header on print.
  - Expand main content area for print (`print:ml-0`, compact print paddings).
- Updated invoice detail page print behavior:
  - Hide action buttons section during print.
  - Render print-only invoice header.
  - Remove card borders/padding for cleaner printed document.
- Commit pushed: `6174036` (`Fix invoice print layout: hide admin chrome and print invoice-only`).
- Deployed to production via Coolify deployment id `92`; status reached `finished`.
